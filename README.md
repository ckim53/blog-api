# Coffee Break - Blog API

RESTful API built with Node.js, Express, and PostgreSQL to manage posts, comments, and users with secure JWT authentication.

## Testing & CI

This project includes backend tests written with Jest and Supertest. These cover authentication, authorization, core API endpoints, and error handling.
GitHub Actions runs the test suite automatically on every push and pull request.
